---
import ProjectLayout from '../../layouts/ProjectLayout.astro';
import { getSessionUser } from '../../firebase/server';

const user = await getSessionUser(Astro.cookies);
if(!user) {
  return Astro.redirect("/signin");
}
---

<ProjectLayout title="Progress">

</ProjectLayout>